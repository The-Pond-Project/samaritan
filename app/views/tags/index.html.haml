%h1 Listing tags

%table
  %thead
    %tr
      %th Name
      %th Description
      %th Organization
      %th Approved
      %th
      %th
      %th

  %tbody
    - @tags.each do |tag|
      %tr
        %td= tag.name
        %td= tag.description
        %td= tag.organization.name
        %td= tag.approved
        %td= link_to 'Show', tag_path(tag.name)

%br

= link_to 'New Tag', new_tag_path
