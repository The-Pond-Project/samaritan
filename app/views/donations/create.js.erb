const stripe_publishable_key = document.querySelector('meta[name="stripe-public-key"]').getAttribute('content')
const stripe = Stripe(stripe_publishable_key)
stripe.redirectToCheckout({
  sessionId: "<%= @session_id %>"
})